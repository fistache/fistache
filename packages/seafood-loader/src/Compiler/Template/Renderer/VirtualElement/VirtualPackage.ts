import { VirtualRenderableInterface } from './Interfaces/VirtualRenderableInterface'
import { VirtualObject } from './VirtualObject'

export class VirtualPackage extends VirtualObject implements VirtualRenderableInterface {
    public render(): void {
        //
    }
}
